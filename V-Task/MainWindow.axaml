<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:V_Task"
        xmlns:models="clr-namespace:V_Task.Models"
        mc:Ignorable="d" d:DesignWidth="900" d:DesignHeight="700"
        x:Class="V_Task.MainWindow"
        x:DataType="local:MainWindow"
        Title="V-Task - Монітор Ресурсів"
        WindowStartupLocation="CenterScreen"
        SystemDecorations="None"
        Width="950"
        Height="750"
        MinWidth="800"
        MinHeight="600"
        CanResize="True"
        UseLayoutRounding="True"
        FontFamily="Segoe UI, Arial, Tahoma"
        Background="#FFF5E6D3">

    <!-- ╔══════════════════════════════════════════════════════════════════════╗ -->
    <!-- ║                         LOCAL RESOURCES                               ║ -->
    <!-- ╚══════════════════════════════════════════════════════════════════════╝ -->
    <Window.Resources>
        
        <!-- Base Colors -->
        <Color x:Key="AppBgColor">#FFF5E6D3</Color>
        <Color x:Key="AppSurfaceColor">#FFFDF7EF</Color>
        <Color x:Key="AppBorderColor">#FFE8D5B7</Color>
        <Color x:Key="AppTextColor">#FF404040</Color>
        <Color x:Key="AppTextMutedColor">#FF606060</Color>

        <!-- Accent Colors -->
        <Color x:Key="AccentOrangeColor">#FFFF8C00</Color>
        <Color x:Key="AccentOrangeHoverColor">#FFFFA000</Color>
        <Color x:Key="AccentPurpleColor">#7D2AE8</Color>
        <Color x:Key="AccentPurpleHoverColor">#A78BFA</Color>

        <!-- Status Colors -->
        <Color x:Key="SuccessColor">#FF10B981</Color>
        <Color x:Key="WarningColor">#FFF59E0B</Color>
        <Color x:Key="DangerColor">#FFEF4444</Color>

        <!-- Solid Color Brushes -->
        <SolidColorBrush x:Key="AppBgBrush" Color="{StaticResource AppBgColor}" />
        <SolidColorBrush x:Key="AppSurfaceBrush" Color="{StaticResource AppSurfaceColor}" />
        <SolidColorBrush x:Key="AppBorderBrush" Color="{StaticResource AppBorderColor}" />
        <SolidColorBrush x:Key="AppTextBrush" Color="{StaticResource AppTextColor}" />
        <SolidColorBrush x:Key="AppTextMutedBrush" Color="{StaticResource AppTextMutedColor}" />

        <SolidColorBrush x:Key="AccentOrangeBrush" Color="{StaticResource AccentOrangeColor}" />
        <SolidColorBrush x:Key="AccentOrangeHoverBrush" Color="{StaticResource AccentOrangeHoverColor}" />
        <SolidColorBrush x:Key="AccentPurpleBrush" Color="{StaticResource AccentPurpleColor}" />
        <SolidColorBrush x:Key="AccentPurpleHoverBrush" Color="{StaticResource AccentPurpleHoverColor}" />

        <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}" />
        <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}" />
        <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColor}" />

        <!-- Gray Gradient for Title Bar -->
        <LinearGradientBrush x:Key="GrayGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FFE0E0E0" Offset="0"/>
            <GradientStop Color="#FFD0D0D0" Offset="0.3"/>
            <GradientStop Color="#FFC5C5C5" Offset="0.7"/>
            <GradientStop Color="#FFC0C0C0" Offset="1"/>
        </LinearGradientBrush>
        
    </Window.Resources>

    <!-- ╔══════════════════════════════════════════════════════════════════════╗ -->
    <!-- ║                          LOCAL STYLES                                 ║ -->
    <!-- ╚══════════════════════════════════════════════════════════════════════╝ -->
    <Window.Styles>
        
        <!-- Window Control Buttons -->
        <Style Selector="Button.window-control">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#303030"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style Selector="Button.window-control:pointerover">
            <Setter Property="Background" Value="#40FFFFFF"/>
        </Style>
        <Style Selector="Button.window-control.close:pointerover">
            <Setter Property="Background" Value="#40FF5C5C"/>
        </Style>

        <!-- Panel Style -->
        <Style Selector="Border.panel">
            <Setter Property="Background" Value="{StaticResource AppSurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AppBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
        </Style>

        <!-- Tab Button Style -->
        <Style Selector="Button.tab-button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource AppTextMutedBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="CornerRadius" Value="8"/>
        </Style>
        <Style Selector="Button.tab-button:pointerover">
            <Setter Property="Background" Value="#20000000"/>
            <Setter Property="Foreground" Value="{StaticResource AppTextBrush}"/>
        </Style>
        <Style Selector="Button.tab-button.active">
            <Setter Property="Background" Value="{StaticResource AccentPurpleBrush}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style Selector="Button.tab-button.active:pointerover">
            <Setter Property="Background" Value="{StaticResource AccentPurpleHoverBrush}"/>
        </Style>

        <!-- Action Button Style -->
        <Style Selector="Button.action-button">
            <Setter Property="Background" Value="{StaticResource AccentOrangeBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="12,6"/>
        </Style>
        <Style Selector="Button.action-button:pointerover">
            <Setter Property="Background" Value="{StaticResource AccentOrangeHoverBrush}"/>
        </Style>
        <Style Selector="Button.action-button:pressed">
            <Setter Property="Background" Value="#FFE07800"/>
        </Style>

        <!-- Danger Button Style -->
        <Style Selector="Button.danger-button">
            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>
        <Style Selector="Button.danger-button:pointerover">
            <Setter Property="Background" Value="#FFDC2626"/>
        </Style>

        <!-- DataGrid Style -->
        <Style Selector="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="RowBackground" Value="{StaticResource AppSurfaceBrush}"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource AppBorderBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
        </Style>

        <!-- Progress Bar Style -->
        <Style Selector="ProgressBar">
            <Setter Property="Height" Value="8"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Background" Value="#FFE8D5B7"/>
        </Style>
        
    </Window.Styles>

    <!-- ╔══════════════════════════════════════════════════════════════════════╗ -->
    <!-- ║                         MAIN CONTENT                                  ║ -->
    <!-- ╚══════════════════════════════════════════════════════════════════════╝ -->
    
    <Border Background="Transparent" Margin="2" BorderThickness="1" BorderBrush="Transparent">
        <Grid RowDefinitions="42,Auto,*">
            <Grid.Background>
                <SolidColorBrush Color="{StaticResource AppBgColor}"/>
            </Grid.Background>

            <!-- ════════════════════════════════════════════════════════════════ -->
            <!-- ROW 0: TITLE BAR                                                 -->
            <!-- ════════════════════════════════════════════════════════════════ -->
            <Grid Grid.Row="0" Name="TitleBar" Background="{StaticResource GrayGradient}">
                <Grid ColumnDefinitions="Auto,*,Auto">

                    <!-- Left: Icon + Title -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                        <!-- App Icon -->
                        <Border Width="28" Height="28" CornerRadius="6" Background="{StaticResource AccentPurpleBrush}" Margin="0,0,10,0">
                            <Image Source="avares://V-Task/Icons/AppIcon.png" Width="20" Height="20"/>
                        </Border>
                        <TextBlock Name="WindowTitle" Text="V-Task - Монітор Ресурсів"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource AppTextBrush}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Middle: Draggable Space -->
                    <Panel Name="TitleBarDragArea" Grid.Column="1" Background="Transparent" PointerPressed="TitleBar_PointerPressed"/>

                    <!-- Right: Window Controls -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Stretch">
                        <Button Name="AgreementButton" Classes="window-control" Padding="0" Width="46" Height="42" Click="AgreementButton_Click" ToolTip.Tip="Угода користувача">
                            <PathIcon Data="M12,1L3,5V11C3,16.55 6.84,21.74 12,23C17.16,21.74 21,16.55 21,11V5L12,1M12,5A3,3 0 0,1 15,8A3,3 0 0,1 12,11A3,3 0 0,1 9,8A3,3 0 0,1 12,5M17.13,17C15.92,18.85 14.11,20.24 12,20.92C9.89,20.24 8.08,18.85 6.87,17C6.53,16.5 6.24,16 6,15.47C6,13.82 8.71,12.47 12,12.47C15.29,12.47 18,13.79 18,15.47C17.76,16 17.47,16.5 17.13,17Z" Width="16" Height="16" Foreground="#303030"/>
                        </Button>
                        <Button Name="SettingsButton" Classes="window-control" Padding="0" Width="46" Height="42" Click="SettingsButton_Click" ToolTip.Tip="Налаштування">
                            <PathIcon Data="M12,15.5A3.5,3.5 0 0,1 8.5,12A3.5,3.5 0 0,1 12,8.5A3.5,3.5 0 0,1 15.5,12A3.5,3.5 0 0,1 12,15.5M19.43,12.97C19.47,12.65 19.5,12.33 19.5,12C19.5,11.67 19.47,11.34 19.43,11L21.54,9.37C21.73,9.22 21.78,8.95 21.66,8.73L19.66,5.27C19.54,5.05 19.27,4.96 19.05,5.05L16.56,6.05C16.04,5.66 15.5,5.32 14.87,5.07L14.5,2.42C14.46,2.18 14.25,2 14,2H10C9.75,2 9.54,2.18 9.5,2.42L9.13,5.07C8.5,5.32 7.96,5.66 7.44,6.05L4.95,5.05C4.73,4.96 4.46,5.05 4.34,5.27L2.34,8.73C2.21,8.95 2.27,9.22 2.46,9.37L4.57,11C4.53,11.34 4.5,11.67 4.5,12C4.5,12.33 4.53,12.65 4.57,12.97L2.46,14.63C2.27,14.78 2.21,15.05 2.34,15.27L4.34,18.73C4.46,18.95 4.73,19.03 4.95,18.95L7.44,17.94C7.96,18.34 8.5,18.68 9.13,18.93L9.5,21.58C9.54,21.82 9.75,22 10,22H14C14.25,22 14.46,21.82 14.5,21.58L14.87,18.93C15.5,18.67 16.04,18.34 16.56,17.94L19.05,18.95C19.27,19.03 19.54,18.95 19.66,18.73L21.66,15.27C21.78,15.05 21.73,14.78 21.54,14.63L19.43,12.97Z" Width="16" Height="16" Foreground="#303030"/>
                        </Button>
                        <Button Name="MinimizeButton" Classes="window-control" Padding="0" Width="46" Height="42" Click="MinimizeButton_Click">
                            <Image Source="avares://V-Task/Icons/MinButton.png" Width="36" Height="36"/>
                        </Button>
                        <Button Name="CloseButton" Classes="window-control close" Padding="0" Width="46" Height="42" Click="CloseButton_Click">
                            <Image Source="avares://V-Task/Icons/CloseButton.png" Width="36" Height="36"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>

            <!-- ════════════════════════════════════════════════════════════════ -->
            <!-- ROW 1: TAB NAVIGATION                                            -->
            <!-- ════════════════════════════════════════════════════════════════ -->
            <Border Grid.Row="1" Classes="panel" Margin="12,12,12,8" Padding="8">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Name="TabDashboard" Classes="tab-button active" Content="📊 Dashboard" Click="TabButton_Click"/>
                    <Button Name="TabMemory" Classes="tab-button" Content="💾 Memory" Click="TabButton_Click"/>
                    <Button Name="TabGPU" Classes="tab-button" Content="🎮 GPU" Click="TabButton_Click"/>
                </StackPanel>
            </Border>

            <!-- ════════════════════════════════════════════════════════════════ -->
            <!-- ROW 2: CONTENT AREA (TAB PANELS)                                 -->
            <!-- ════════════════════════════════════════════════════════════════ -->
            <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Panel Margin="12,4,12,12">
                    
                    <!-- ═══════════════════════════════════════════════════════════ -->
                    <!-- DASHBOARD PANEL                                             -->
                    <!-- ═══════════════════════════════════════════════════════════ -->
                    <Border Name="PanelDashboard" Classes="panel" Padding="16" IsVisible="True">
                        <Grid RowDefinitions="Auto,Auto,Auto" ColumnDefinitions="*,*">
                            
                            <!-- CPU Card (static) -->
                            <Border Grid.Row="0" Grid.Column="0" Classes="panel" Margin="4" Padding="12">
                                <StackPanel>
                                    <TextBlock Name="DashCpuLabel" Text="🧠 CPU" FontSize="18" FontWeight="Bold" Foreground="{StaticResource AppTextBrush}" Margin="0,0,0,8"/>
                                    <TextBlock x:Name="DashCpuName" Text="Виявлення процесора…" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}" TextWrapping="Wrap" Margin="0,0,0,4"/>
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <TextBlock Name="DashCpuCoresLabel" Text="Ядра (фіз/лог):" FontSize="11" Foreground="{StaticResource AppTextMutedBrush}"/>
                                        <TextBlock x:Name="DashCpuCores" Text="—" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource AppTextBrush}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- RAM Card -->
                            <Border Grid.Row="0" Grid.Column="1" Classes="panel" Margin="4" Padding="12">
                                <StackPanel>
                                    <TextBlock Name="DashRamLabel" Text="💾 Memory" FontSize="18" FontWeight="Bold" Foreground="{StaticResource AppTextBrush}" Margin="0,0,0,8"/>
                                    <TextBlock Name="DashRamTotal" Text="Розрахунок…" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}" Margin="0,0,0,4"/>
                                    <TextBlock Name="DashRamUsage" Text="0%" FontSize="36" FontWeight="Bold" Foreground="{StaticResource AccentPurpleBrush}" Margin="0,4"/>
                                    <ProgressBar Name="DashRamProgress" Value="0" Maximum="100" Margin="0,4"/>
                                    <Grid ColumnDefinitions="*,*" Margin="0,8,0,0">
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Name="DashRamUsedLabel" Text="Використано" FontSize="11" Foreground="{StaticResource AppTextMutedBrush}"/>
                                            <TextBlock Name="DashRamUsed" Text="0 GB" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource AppTextBrush}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Name="DashRamAvailLabel" Text="Доступно" FontSize="11" Foreground="{StaticResource AppTextMutedBrush}"/>
                                            <TextBlock Name="DashRamAvail" Text="0 GB" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource AppTextBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- Disk Card -->
                            <Border Grid.Row="1" Grid.Column="0" Classes="panel" Margin="4" Padding="12">
                                <StackPanel>
                                    <TextBlock Name="DashDiskLabel" Text="💿 Disk" FontSize="18" FontWeight="Bold" Foreground="{StaticResource AppTextBrush}" Margin="0,0,0,8"/>
                                    <TextBlock Name="DashDiskName" Text="C:\\" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}" Margin="0,0,0,4"/>
                                    <TextBlock Name="DashDiskUsage" Text="0%" FontSize="36" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}" Margin="0,4"/>
                                    <ProgressBar Name="DashDiskProgress" Value="0" Maximum="100" Margin="0,4"/>
                                    <Grid ColumnDefinitions="*,*" Margin="0,8,0,0">
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Name="DashDiskUsedLabel" Text="Використано" FontSize="11" Foreground="{StaticResource AppTextMutedBrush}"/>
                                            <TextBlock Name="DashDiskUsed" Text="0 GB" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource AppTextBrush}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Name="DashDiskTotalLabel" Text="Всього" FontSize="11" Foreground="{StaticResource AppTextMutedBrush}"/>
                                            <TextBlock Name="DashDiskTotal" Text="0 GB" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource AppTextBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- Network Card -->
                            <Border Grid.Row="1" Grid.Column="1" Classes="panel" Margin="4" Padding="12">
                                <StackPanel>
                                    <TextBlock Name="DashNetLabel" Text="🌐 Network" FontSize="18" FontWeight="Bold" Foreground="{StaticResource AppTextBrush}" Margin="0,0,0,4"/>
                                    <TextBlock Name="DashNetName" Text="Loading..." FontSize="11" Foreground="{StaticResource AppTextMutedBrush}" Margin="0,0,0,4"/>
                                    
                                    <!-- Combined Speed -->
                                    <Grid ColumnDefinitions="*,*" Margin="0,4,0,4">
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Name="DashNetDownLabel" Text="↓ Завантаження" FontSize="10" Foreground="{StaticResource AppTextMutedBrush}"/>
                                            <TextBlock Name="DashNetDown" Text="0 KB/s" FontSize="16" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Name="DashNetUpLabel" Text="↑ Вивантаження" FontSize="10" Foreground="{StaticResource AppTextMutedBrush}"/>
                                            <TextBlock Name="DashNetUp" Text="0 KB/s" FontSize="16" FontWeight="Bold" Foreground="{StaticResource AccentOrangeBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                    
                                    <!-- WiFi Interface -->
                                    <Border Background="#15FFFFFF" CornerRadius="4" Padding="6" Margin="0,4,0,2">
                                        <Grid ColumnDefinitions="*,Auto,Auto">
                                            <TextBlock Name="DashNetWifiName" Grid.Column="0" Text="📶 Wi-Fi" FontSize="10" Foreground="{StaticResource AppTextMutedBrush}" VerticalAlignment="Center"/>
                                            <TextBlock Name="DashNetWifiDown" Grid.Column="1" Text="0 KB/s" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource SuccessBrush}" Margin="8,0" VerticalAlignment="Center"/>
                                            <TextBlock Name="DashNetWifiUp" Grid.Column="2" Text="0 KB/s" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource AccentOrangeBrush}" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                    
                                    <!-- Ethernet Interface -->
                                    <Border Background="#15FFFFFF" CornerRadius="4" Padding="6" Margin="0,2,0,4">
                                        <Grid ColumnDefinitions="*,Auto,Auto">
                                            <TextBlock Name="DashNetEthName" Grid.Column="0" Text="🔌 Ethernet" FontSize="10" Foreground="{StaticResource AppTextMutedBrush}" VerticalAlignment="Center"/>
                                            <TextBlock Name="DashNetEthDown" Grid.Column="1" Text="0 KB/s" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource SuccessBrush}" Margin="8,0" VerticalAlignment="Center"/>
                                            <TextBlock Name="DashNetEthUp" Grid.Column="2" Text="0 KB/s" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource AccentOrangeBrush}" VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                    
                                    <!-- Totals -->
                                    <Grid ColumnDefinitions="*,*" Margin="0,2,0,0">
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Name="DashNetReceivedLabel" Text="Отримано" FontSize="10" Foreground="{StaticResource AppTextMutedBrush}"/>
                                            <TextBlock Name="DashNetTotalDown" Text="0 MB" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource AppTextBrush}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Name="DashNetSentLabel" Text="Відправлено" FontSize="10" Foreground="{StaticResource AppTextMutedBrush}"/>
                                            <TextBlock Name="DashNetTotalUp" Text="0 MB" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource AppTextBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- System Info Card -->
                            <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Classes="panel" Margin="4" Padding="12">
                                <Grid ColumnDefinitions="*,*">
                                    <StackPanel Grid.Column="0" Margin="8,0">
                                        <TextBlock Name="DashSystemLabel" Text="💻 Система" FontSize="14" FontWeight="Bold" Foreground="{StaticResource AppTextBrush}" Margin="0,0,0,8"/>
                                        <TextBlock Name="DashSystemOS" Text="Windows 11" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}" Margin="0,2"/>
                                        <TextBlock Name="DashSystemUptime" Text="Uptime: 0h 0m" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}" Margin="0,2"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Margin="8,0">
                                        <TextBlock Name="DashUpdateTimeLabel" Text="🕐 Час оновлення" FontSize="14" FontWeight="Bold" Foreground="{StaticResource AppTextBrush}" Margin="0,0,0,8"/>
                                        <TextBlock Name="DashUpdateTime" Text="1 секунда" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}" Margin="0,2"/>
                                        <Button Name="BtnRefreshDash" Classes="action-button" Content="Оновити" Margin="0,4,0,0" Click="RefreshDashboard_Click"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- ═══════════════════════════════════════════════════════════ -->
                    <!-- MEMORY PANEL                                                -->
                    <!-- ═══════════════════════════════════════════════════════════ -->
                    <Border Name="PanelMemory" Classes="panel" Padding="16" IsVisible="False">
                        <StackPanel>
                            <TextBlock Name="MemDetailsLabel" Text="💾 Детальна інформація про пам'ять" FontSize="20" FontWeight="Bold" Foreground="{StaticResource AppTextBrush}" Margin="0,0,0,12"/>
                            
                            <!-- RAM Info -->
                            <Border Classes="panel" Padding="12" Margin="0,0,0,12">
                                <Grid RowDefinitions="Auto,Auto,Auto">
                                    <TextBlock Grid.Row="0" Name="MemRamLabel" Text="Оперативна пам'ять (RAM)" FontSize="16" FontWeight="Bold" Foreground="{StaticResource AppTextBrush}" Margin="0,0,0,12"/>
                                    <Grid Grid.Row="1" ColumnDefinitions="*,*,*,*">
                                        <StackPanel Grid.Column="0" Margin="8">
                                            <TextBlock Name="MemTotalLabel" Text="Всього" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}"/>
                                            <TextBlock Name="MemTotalRam" Text="0 GB" FontSize="20" FontWeight="Bold" Foreground="{StaticResource AppTextBrush}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Margin="8">
                                            <TextBlock Name="MemUsedLabel" Text="Використано" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}"/>
                                            <TextBlock Name="MemUsedRam" Text="0 GB" FontSize="20" FontWeight="Bold" Foreground="{StaticResource AccentPurpleBrush}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2" Margin="8">
                                            <TextBlock Name="MemAvailLabel" Text="Доступно" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}"/>
                                            <TextBlock Name="MemAvailRam" Text="0 GB" FontSize="20" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="3" Margin="8">
                                            <TextBlock Name="MemUsageLabel" Text="Використання" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}"/>
                                            <TextBlock Name="MemUsageRam" Text="0%" FontSize="20" FontWeight="Bold" Foreground="{StaticResource AccentOrangeBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                    <ProgressBar Grid.Row="2" Name="MemRamProgress" Value="0" Maximum="100" Height="16" Margin="8,12,8,0"/>
                                </Grid>
                            </Border>

                            <!-- Swap/Page File Info -->
                            <Border Classes="panel" Padding="12" Margin="0,0,0,12">
                                <Grid RowDefinitions="Auto,Auto,Auto">
                                    <TextBlock Grid.Row="0" Name="MemSwapLabel" Text="Файл підкачки (Swap / Page File)" FontSize="16" FontWeight="Bold" Foreground="{StaticResource AppTextBrush}" Margin="0,0,0,12"/>
                                    <Grid Grid.Row="1" ColumnDefinitions="*,*,*,*">
                                        <StackPanel Grid.Column="0" Margin="8">
                                            <TextBlock Name="MemSwapTotalLabel" Text="Всього" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}"/>
                                            <TextBlock Name="MemTotalSwap" Text="0 GB" FontSize="20" FontWeight="Bold" Foreground="{StaticResource AppTextBrush}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Margin="8">
                                            <TextBlock Name="MemSwapUsedLabel" Text="Використано" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}"/>
                                            <TextBlock Name="MemUsedSwap" Text="0 GB" FontSize="20" FontWeight="Bold" Foreground="{StaticResource WarningBrush}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2" Margin="8">
                                            <TextBlock Name="MemSwapAvailLabel" Text="Доступно" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}"/>
                                            <TextBlock Name="MemAvailSwap" Text="0 GB" FontSize="20" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="3" Margin="8">
                                            <TextBlock Name="MemSwapUsageLabel" Text="Використання" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}"/>
                                            <TextBlock Name="MemUsageSwap" Text="0%" FontSize="20" FontWeight="Bold" Foreground="{StaticResource AccentOrangeBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                    <ProgressBar Grid.Row="2" Name="MemSwapProgress" Value="0" Maximum="100" Height="16" Margin="8,12,8,0"/>
                                </Grid>
                            </Border>

                            <!-- Memory Details -->
                            <Border Classes="panel" Padding="12">
                                <StackPanel Margin="8">
                                    <TextBlock Name="MemAdditionalInfoLabel" Text="📊 Додаткова інформація" FontSize="14" FontWeight="Bold" Foreground="{StaticResource AppTextBrush}" Margin="0,0,0,8"/>
                                    <TextBlock Name="MemSpeed" Text="Частота: 0 MHz" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}" Margin="0,2"/>
                                    <TextBlock Name="MemType" Text="Тип: DDR4" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}" Margin="0,2"/>
                                    <TextBlock Name="MemSlots" Text="Слоти: 0 / 0" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}" Margin="0,2"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>


                    <!-- ═══════════════════════════════════════════════════════════ -->
                    <!-- GPU PANEL                                                   -->
                    <!-- ═══════════════════════════════════════════════════════════ -->
                    <Border Name="PanelGPU" Classes="panel" Padding="16" IsVisible="False">
    <StackPanel>
        <TextBlock Name="GpuDetailsLabel" Text="🎮 Детальна інформація про GPU" FontSize="20" FontWeight="Bold" Foreground="{StaticResource AppTextBrush}" Margin="0,0,0,12"/>

        <!-- Multi-GPU list -->
        <ItemsControl Name="GpuCards" Margin="0,0,0,12">
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="models:GpuAdapterInfo">
                    <Border Classes="panel" Padding="12" Margin="0,0,0,12">
                        <Grid RowDefinitions="Auto,Auto,Auto" ColumnDefinitions="*,Auto">
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="8,0,8,8">
                                <TextBlock Text="{Binding ., Converter={x:Static local:GpuCardTypeConverter.Instance}}" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}"/>
                                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" Foreground="{StaticResource AppTextBrush}" TextWrapping="Wrap"/>
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock Text="{Binding ., Converter={x:Static local:LocalizedStringConverter.Instance}, ConverterParameter=Driver}" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}"/>
                                    <TextBlock Text=" " FontSize="12"/>
                                    <TextBlock Text="{Binding DriverVersion}" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}"/>
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="8,0,8,8" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding ., Converter={x:Static local:LocalizedStringConverter.Instance}, ConverterParameter=GPUMemory}" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}" HorizontalAlignment="Right"/>
                                <TextBlock Text="{Binding TotalMemoryGB, StringFormat={}{0:F1} GB}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource AccentPurpleBrush}" HorizontalAlignment="Right"/>
                            </StackPanel>

                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="8,0,8,8">
                                <TextBlock Text="{Binding ., Converter={x:Static local:LocalizedStringConverter.Instance}, ConverterParameter=Status}" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}"/>
                                <TextBlock Text="{Binding IsActive, Converter={x:Static local:GpuStatusTextConverter.Instance}}" FontSize="16" FontWeight="SemiBold" Foreground="{Binding IsActive, Converter={x:Static local:StatusColorConverter.Instance}}"/>
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="8">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding ., Converter={x:Static local:LocalizedStringConverter.Instance}, ConverterParameter=GpuUsed}" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}"/>
                                    <TextBlock Text=" " FontSize="12"/>
                                    <TextBlock Text="{Binding UsedMemoryGB, StringFormat={}{0:F1} GB}" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}"/>
                                </StackPanel>
                                <ProgressBar Value="{Binding UsagePercent}" Maximum="100" Height="10" Margin="0,6,0,0"/>
                                <TextBlock Text="{Binding UsagePercent, StringFormat={}{0:F1}%}" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>


        <!-- Additional GPU Info -->
        <Border Classes="panel" Padding="12">
            <StackPanel Margin="8">
                <TextBlock Name="GpuTechDataLabel" Text="🔧 Технічні дані" FontSize="14" FontWeight="Bold" Foreground="{StaticResource AppTextBrush}" Margin="0,0,0,8"/>
                <TextBlock Name="GpuDriver" Text="Драйвер: Н/Д" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}" Margin="0,2"/>
                <TextBlock Name="GpuBusInterface" Text="Інтерфейс: Н/Д" FontSize="12" Foreground="{StaticResource AppTextMutedBrush}" Margin="0,2"/>
            </StackPanel>
        </Border>
    </StackPanel>
</Border>

                </Panel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
